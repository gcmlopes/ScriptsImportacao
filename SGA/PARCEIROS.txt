select partrazao as NOMERAZAO, 
partcnpjcpf AS CPFCNPJ, 
partierg AS RGIE, 
partfantasia AS APELIDOFANTASIA, 
partcep AS CEP,
partlogradouro as ENDERECO,
partnro as NUMERO,
partcomplemento as COMPLEMENTO,
partbairro as BAIRRO,
partcidade as IBGE,
'' as CIDADE,
'' as UF,
concat(partfoneddd, partfonepre, partfonesuf) as TELEFONE,
partemail as EMAIL,
'' as OBSERVACAO,
'' as MENSAGEM,
CASE WHEN part_cli = 'Sim' THEN 'S' ELSE 'N' END AS CLIENTEVISTA,
CASE WHEN part_cli = 'Sim' THEN 'S' ELSE 'N' END AS CLIENTEPRAZO,
CASE WHEN part_for = 'Sim' THEN 'S' ELSE 'N' END AS FORNECEDOR,
'N' AS CLIENTECHEQUE,
CASE WHEN part_fun = 'Sim' THEN 'S' ELSE 'N' END AS FUNCIONARIO,
partlimite as LIMITEONLINE,
partlimite as LIMITEOFFLINE
FROM PART